name: workflow-for on premise server

on: 
  push:

jobs:
  TEST-JOB:
    runs-on: self-hosted

    steps:  # 👈 This was missing!
      - name: Checkout the main branch
        uses: actions/checkout@v3

      - name: Print working directory
        run: pwd

      - name: List all files
        run: ls -a

      - name: Print hello
        run: echo "hello world" >> hello.txt

  build-job:
    needs: [TEST-JOB]
    runs-on: self-hosted

    steps:  # 👈 This was also missing!
      - name: Check permissions on directory
        run: ls -lrt
# i have jsut updated the run.sh file and to run itin the bg mode 